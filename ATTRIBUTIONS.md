# Attributions

KoalaBye serves application assets locally and does not depend on external asset CDNs.

The flag SVGs in `web/static/flags/` are original project artwork and do not contain third-party assets.

Third-party Go and JavaScript dependencies remain subject to their respective licenses as recorded by their upstream projects and the repository dependency manifests.
